# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.code-workspace
.worktrees/

# Stray dependency / tool caches that some commands accidentally create
# at the repo root instead of inside their workspace
node_modules/
.vite/

# Reference deployment files from another project (CheckPrint), kept local
docs/reference-dploiement/
.wt/

# Maquettes de brainstorming (superpowers) — locales, non versionnées
.superpowers/

# Environnements des piles Compose (ADR-0028) : générés en local par les tests de fumée, portés
# par Dokploy sur le serveur. Jamais versionnés — seuls les *.example le sont.
deploy/**/.env
